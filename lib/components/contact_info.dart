import 'package:flutter/material.dart';

class ContactAvatar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Contact Avatar Component"),
    );
  }
}
